# Log — rust-multi-platform

## [2026-05-21] init | new topic wiki created

## [2026-05-21] research | "Rust multi-platform" --deep (topic mode, 4 scopes: mobile FFI, desktop cross-compile, UI frameworks, WASM) → 8 agents, 29 sources ingested (16 articles, 12 repos, 1 guide), 6 articles compiled (1 topic synthesis + 5 concept). Critical findings: iroh-ffi archived mid-2025; Tauri 2 mobile has unresolved Xcode 26 link errors + WKWebView regressions; Dioxus mobile is dioxus-desktop re-exported (webview); Slint iOS = tech-preview only; mobile a11y unsupported across all Rust UI frameworks; Microsoft Trusted Signing changes Windows-signing economics (~$700/yr EV → ~$120/yr); Fermyon acquired by Akamai.

## [2026-05-21] librarian | scanned 6 articles, 6 stale (structural — no `sources:` frontmatter), 0 low-quality

## [2026-05-21] librarian | backfilled `sources:` frontmatter on all 6 articles; added `verified: 2026-05-21` and `volatility` field; synthesis tagged `compiled-from: conversation`

## [2026-05-23] librarian | re-scanned 6 articles, 0 stale, 0 low-quality (avg staleness 99, avg quality 93). Structural fix landed. 3 broken cross-refs persist (`browser-wasm-frontend-frameworks` x1, `release-pipeline-canonical-2026` x2).

## [2026-06-01] assess (cross-link) | herd-scout livestock-CV product assessed primarily against `gtx-1060-headless-ai-server` (CV is more central); this wiki contributed 3 build opportunities (cargo-dist desktop release matrix; UniFFI migration for non-perf-critical Android FFI surfaces while keeping `jni-rs` for camera/video hot path; iOS admin app via UniFFI + xcframework + SPM). Report: gtx-1060-headless-ai-server/output/assess-herd-scout-2026-06-01.md.

## [2026-06-01] research --deep | rust-multi-platform · "Edge fleet operational patterns 2026: single-slot fleet identity (Tailscale tagged-authkey, K8s StatefulSet, Balena, Mender prior art); versioned signed identity envelopes (TOML/CBOR/Protobuf for long-lived ed25519 with rotation); append-only audit logs over edge RPC" → 8 agents (Academic, Technical, Applied, News/Trends, Contrarian, Historical, Adjacent, Data/Stats), 43 new sources ingested (24 articles, 6 repos, 4 guides, 8 papers, 1 data — total wiki now 72 sources), 4 articles compiled (1 topic synthesis + 3 concept). Headline findings: (a) the 4 prior-art systems converge on a two-tier secret pattern (fleet provisioning key → per-device key) and a (hardware-derived identity attribute + rotatable keypair) split; Mender's `mender-device-identity` script is the cleanest formalization. (b) **TOML cannot be the signed format** — no canonical form (ALF #397). Sign JCS-canonical JSON, deterministic CBOR (RFC 8949 §4.2), or protobuf wire format. TOML is fine as human-edited source if you parse-then-canonicalize. (c) Three-layer URI versioning (in-toto pattern: envelope-protocol prefix + statement schema URI + predicate URI) is the right shape for long-lived envelopes. (d) Two rotation idioms — dual-sign chain (TUF, DSSE multi-sig) for offline/intermittent edge; short-lived cert under long-lived root (Sigstore) for cloud-side. Cosign v3 + sigstore-c (Apr 2026) close the embedded-verification gap. (e) Pin Ed25519-IETF (RFC 8032) explicitly — three security-distinct variants exist (Brendel et al. 2021); pin one verifier impl across server and edge to dodge library-divergence bugs (Cendyne 2022; Tor + ZCash incidents). (f) Append-only is a property of the data structure, not the system — without witnesses or sufficient gossip a malicious server can fork views (split-view attack). For sub-100-device fleets, mirror to Sigstore Rekor for free witnesses-as-a-service. (g) Tile-backed transparency logs (Tessera/Rekor v2 GA Oct 2025) replace Trillian in 2026 builds; MMD = 60 sec achievable. (h) Tailscale Workload Identity Federation went GA Feb 19 2026 — eliminates static OAuth secrets in CI; SpecterOps documents the OAuth-secret exfil playbook against the older pattern. (i) Mender SD-card-clone failure mode is real: identity must be sealed to non-cloneable hardware (TPM/fuse/SoC unique ID), not "a file in /var/lib". (j) Audit-log throughput is not a bottleneck — Rekor ~2.2 entries/sec mean; 1000-device fleet × 1 event/min = ~17 events/sec, ~12% of Cloudflare Nimbus's CT write rate. Open gaps for follow-up: Rust crate maturity for COSE/DSSE/Tessera; iroh + SCITT integration; TPM-backed ed25519 in cross-platform Rust; Schneier-Kelsey on-device log Rust impl; small-fleet witness playbook.

## [2026-06-02] research | rust-multi-platform · "Loro vs y-crdt for Rust-native mobile CRDT sync 2026" (1-agent contribution to christ-is-lord assess Phase 1 fanout) → 11 sources ingested (total wiki now 83), 1 concept article compiled (loro-vs-y-crdt-mobile.md). Headline: Loro v1.12.1 is post-1.0, on monthly cadence, with a working UniFFI Swift xcframework (loro-swift v1.10.3, Dec 2025); yrs is still pre-1.0 at v0.18 (March 2025) with no 2025 follow-up, and yswift has been dormant since v0.2.1 (April 2024). Yjs v14 has been an open RC since May 2024, weakening the yrs Yjs-wire-compat case. Recommendation for christ-is-lord v0.5+: spike a Loro-based note-sync prototype behind the existing sync trait; measure binary-size delta on iOS; keep yrs only if Yjs JS interop with non-app clients is hard-required.
