# Log — sv2-coinbase-verify-daemon

## [2026-07-21] init | topic 'sv2-coinbase-verify-daemon' created

## [2026-07-21] research | "a simple daemon that connects to mine to an SV2 pool and checks the coinbase against an expected value" → 5 parallel agents (client-flow / coinbase-structure / Rust-stack / prior-art / trust-model), 13 sources ingested (7 articles, 2 BIPs, 4 repos), 11 articles compiled (8 concepts, 2 topics, 1 reference). Headline findings: (a) **only an extended SV2 channel exposes the coinbase** — `NewMiningJob` (standard) gives an opaque `merkle_root`, so a coinbase check is structurally impossible there; `NewExtendedMiningJob` carries `coinbase_tx_prefix`/`coinbase_tx_suffix`/`merkle_path`. (b) **Reuse SRI**: `channels_sv2::client::extended::ExtendedChannel::validate_share` + `merkle_root_from_path` already do reconstruct→fold→hash→target; fork the `mining-device` skeleton, swap standard→extended. (c) "Expected value" is a family of checks (payout SPK / value / scriptSig tag / BIP34 height / `0xfabe6d6d` / OP_RETURN commitment / root integrity), all needing raw coinbase bytes. (d) **Honest scoping**: passive check = trust-but-verify, not trustless (proves only this job as served to this miner; real trust-min = Job Declaration). No existing tool fills the per-miner-SV2 coinbase-assertion niche. Progress score ~90. Cross-links: stratum-sri, sv2-coinbase-identity, datum, sv2-p2pool-integration, bitcoin-mining-payout-schemas, mining-scale-test-sim.

## [2026-07-21] research r2 (gap-closing, 4 parallel paths) → +5 sources (18 total), +3 articles (14 total) + 1 output playbook. Paths: (1) **reference impl** — source-verified crate versions (stratum-core 0.5, mining_sv2 11, channels_sv2 7, codec_sv2 6, bitcoin 0.32.5, all crates.io), exact struct/fn signatures (SetupConnection, OpenExtendedMiningChannel, ExtendedChannel::new/validate_share, merkle_root_from_path), frame build/parse idioms, honest UNVERIFIED list → new topic reference-implementation-skeleton. (2) **stratum-sniffer** — decisive finding: it's an ACTIVE MITM (own hardcoded keypair 9auqW.../mkDLT..., terminates two Noise sessions), NOT a passive tap; SV2 Noise_NX encryption ⇒ be your own SV2 client. (3) **expected value** — subsidy computable from height (5e9 >> (height/210000) = 312,500,000 sat / 3.125 BTC at ~height 900k; bad-cb-amount ceiling), fees need a template (coinbase_tx_value_remaining), payout target = pool-address (custodial FPPS/PPLNS) vs miner-address (SOLO/DATUM/JD), seeded from bitcoin-data/mempool mining-pools datasets → new concept sourcing-the-expected-value. (4) **deviation detection** — job-diff heuristic (anchor coinbase change to SetNewPrevHash; payout change w/o new prevhash = HIGH), on-chain correlation loop (own bitcoind ZMQ or mempool/Esplora), sybil auditing; block withholding provably undetectable from share stats (APoW arXiv 2601.02496, Optech) → new concept deviation-detection. New articles: reference-implementation-skeleton, sourcing-the-expected-value, deviation-detection; updated sri-client-crate-stack (verified versions + sniffer note) + daemon-build-playbook. Output: playbook-sv2-coinbase-verify-daemon-2026-07-21. Progress ~94.
## [2026-07-21] plan | "small daemon that reconstructs the coinbase tx to check it is valid and pays to a certain address" → output/plan-coinbase-address-check-daemon-2026-07-21.md (9 articles consulted, 5 decisions, 6 phases). Scope (per interview): one-shot check-then-exit, single address any-type via bitcoin::Address, standalone structural+merkle validity (no bitcoind), reuse SRI channels_sv2. Name: cbcheck.
