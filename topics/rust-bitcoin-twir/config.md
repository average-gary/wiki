---
title: Config
type: topic-config
---

# Config — rust-bitcoin-twir

## Sensitivity
public-oss — TWiR is itself CC-BY-SA / public; rust-bitcoin ecosystem projects are public OSS.

## Project root
/Users/garykrause/repos/this-week-in-rust (when researching from there) — note that this is a clone of `rust-lang/this-week-in-rust`, not a fork, so PR origin must be a separate fork.

## Submission policy
- TWiR rejects: paywalled (incl. Medium members-only), bare crate.io/GitHub links, LLM-authored without disclosure, COC violations, info-capture gates.
- TWiR accepts: how-to / deep dive blog posts, walkthroughs, release notes WITH explanation, Rust podcast/video/slides, meetup recordings, CFP/jobs.
- **Project/Tooling Updates** require Rust-specific content (examples, lessons learned, why), not just changelogs. One submission per contributor per week.

## Operational rules
- Only ingest non-paywalled, non-info-gated sources (matches TWiR policy).
- Tag every submission candidate with `twir-fit: <yes/maybe/no>` and `twir-section: <section>`.
- Volatility: news/release sources are HOT (refresh weekly during active issues); crate primitives are WARM; submission-rules concepts are COLD.
